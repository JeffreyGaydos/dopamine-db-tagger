# dopamine-db-tagger
Standing on top of the Dopamine SQLite database, runs users through their tracks in various ways so that they can add more descriptors to their music. After tags are added, users can then export different queries of tags as Android compatible m3u files.
